"""Доменный слой (ядро Clean Architecture).

Содержит агрегат ``Product``, справочные сущности, value objects,
доменные события и доменные исключения. Импортирует ТОЛЬКО stdlib —
никаких фреймворков, ORM, async или I/O (правило зависимостей).
"""
